import { css } from 'linaria';

import { colors } from '../../style-tokens';

export const section = css`
  align-items: center;
  color: ${colors.white};
  display: flex;
  flex-direction: column;
`;
